# Browser configuration
browser.type=chromium
browser.headless=false

# AI Healing (primary config is in healing-config.yaml)
# These can also be set as system properties: -Dhealing.enabled=false
healing.enabled=true
ai.provider=GEMINI
